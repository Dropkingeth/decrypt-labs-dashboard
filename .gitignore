# ================================================
# 🔐 DECRYPT LABS - GITIGNORE
# ================================================
# NEVER commit sensitive files. This file protects you.
# ================================================

# ============ SECRETS & KEYS ============
# Private keys, env files, credentials
.env
.env.*
*.env
.env.local
.env.*.local
*.key
*.pem
*.p12
*private*key*
*secret*
credentials.json
secrets.json

# ============ TRADING STRATEGIES ============
# PineScript files - YOUR SECRET SAUCE
*.pine
*.pinescript
webhook/*.pine
strategies/*.pine
*indicator*.pine
*webhook*.pine
# Allow HTML pages in strategies folder
!**/strategies/index.html

# ============ TRADE DATA & LOGS ============
# Your trading history and performance
*.jsonl
*.log
trades.json
trades.jsonl
stats.json
positions.json
**/logs/
**/*.log

# ============ LOCAL CONFIG ============
# Machine-specific settings
*.local.js
*.local.ts
*.local.json
config.local.*
caretaker/config.local.js

# ============ FOUNDRY / SOLIDITY ============
# Build artifacts (can be regenerated)
cache/
out/
broadcast/
contracts/cache/
contracts/out/
contracts/broadcast/

# ============ NODE / NPM ============
node_modules/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ============ OS FILES ============
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db

# ============ IDE ============
.idea/
.vscode/
*.swp
*.swo
*~

# ============ MISC ============
*.bak
*.backup
*.tmp
*.temp

# ============ LAUNCH / WALLET ============
# NEVER commit wallet keys, launch secrets
launch/*.key
launch/*.json
launch/wallet*
launch/WALLET*
*wallet*private*
*seed*phrase*
*mnemonic*

# ============ ICT PIPELINE ============
# API keys and secrets
ict-ai-analyst/.env
ict-ai-analyst/.env.*
launch/.env
launch/wallet*.json
-e 
# Large video files
pitch-video/
nft-art/
